# Walmart Playbook

- Use canonical product URLs from walmart.com product pages.
- Start with a 20-30 second monitor interval and tune after observing success rates.
- Validate keyword filters against page body text, not marketing labels.
- Use `max_price_cents` to reduce false positives on third-party listings.
